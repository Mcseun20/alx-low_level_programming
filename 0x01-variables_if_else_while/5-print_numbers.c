#include <stdio.h>

/**
 * main - assign variable for n each time it executed
 * print the last digit of stored in variable n
 *
 *Return: 0
 */
int main(void)
{

	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
